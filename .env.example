# =============================================================================
# M2 Desktop - Environment Variables
# =============================================================================

# Authentication (used by all variants)
VNC_PASSWORD=change-me-to-something-secure

# AI API Keys (for OpenClaw Gateway)
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-...

# =============================================================================
# Variant Selection (set automatically by docker-compose files)
# =============================================================================
# M2_VARIANT=guacamole   # guacamole | novnc | selkies

# =============================================================================
# Selkies Variant (WebRTC)
# =============================================================================
# Video encoder (auto-detected: nvh264enc with GPU, x264enc without)
# SELKIES_ENCODER=nvh264enc

# TURN server for NAT traversal (required for external access)
# TURN_HOST=turn.yourdomain.com
# TURN_PORT=3478
# TURN_USERNAME=turnuser
# TURN_PASSWORD=turnpass

# =============================================================================
# Full Apache Guacamole (optional enterprise features)
# =============================================================================
# Database credentials for guacamole-db (MariaDB)
GUAC_DB_ROOT_PASSWORD=change-me-root-password
GUAC_DB_PASSWORD=change-me-user-password

# NOTE: Guacamole web admin user (created in database init):
#   Username: guacadmin
#   Password: guacadmin
# IMPORTANT: Change this password immediately after first login!

# =============================================================================
# Port Configuration (for local/standalone use only)
# =============================================================================
# Guacamole-Lite (guacamole variant)
# GUAC_LITE_PORT=28080

# noVNC (novnc variant)
# NOVNC_PORT=26080

# Selkies WebRTC (selkies variant)
# SELKIES_PORT=28080

# Full Apache Guacamole
# GUAC_FULL_PORT=28888

# OpenClaw Gateway (all variants)
# M2_GATEWAY_PORT=18789

# =============================================================================
# Container Resources
# =============================================================================
# Shared memory size (for browser tabs, video rendering, etc.)
# SHM_SIZE=2gb

# =============================================================================
# Coolify/Traefik Routing (production - no port exposure needed)
# =============================================================================
# g1.yourdomain.com → Guacamole-Lite (internal 8080)
# g2.yourdomain.com → Full Guacamole (internal 8080)
# gateway.yourdomain.com → OpenClaw Gateway (internal 18789)
