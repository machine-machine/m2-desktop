# Traefik Routing (single domain with path-based routing)
# Root (/) → Clawdbot Gateway, /vnc/ → noVNC
DOMAIN=clawd.yourdomain.com

# VNC Access
VNC_PASSWORD=change-me-to-something-secure

# Clawdbot API Keys (optional if using Claude Max browser auth)
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-...
