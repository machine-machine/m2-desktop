# Traefik Routing
# g1.* → Guacamole-Lite (port 8080)
# g2.* → Full Guacamole (port 8888)
DOMAIN=clawd.yourdomain.com

# VNC Access (used by x11vnc and guacamole-lite auth)
VNC_PASSWORD=change-me-to-something-secure

# Clawdbot API Keys (optional if using Claude Max browser auth)
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-...

# =============================================================================
# Full Apache Guacamole Database (g2.* subdomain)
# =============================================================================
# Database credentials for guacamole-db (MariaDB)
GUAC_DB_ROOT_PASSWORD=change-me-root-password
GUAC_DB_PASSWORD=change-me-user-password

# NOTE: The Guacamole web admin user is created in the database init script:
#   Username: guacadmin
#   Password: guacadmin
# IMPORTANT: Change this password immediately after first login via the web UI!
